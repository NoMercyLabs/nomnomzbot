# Spec Gap Audit

Every band this audit ever tracked — BLOCKING (B1–B8b), CROSS-CUTTING (X1–X4), MISSING SPECS
(M1–M3), PRODUCT-EDGE & OPS (P1–P7 + management groups A–F), SHOULD-FIX, and MINOR — has been
**verified resolved against the live spec files and codebase**. Resolved entries are removed, not
archived (history lives in git).

**The live open-work list is `../ROADMAP.md`** — new gaps go there, not here.
