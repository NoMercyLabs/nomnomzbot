# Tool state
.playwright-mcp/

# Dependencies
node_modules/

# Build outputs
bin/
obj/
dist/
out/

# Environment
.env
.env.local
.env.*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vs/
.vscode/
.idea/
*.user
*.suo

# .NET
*.user
*.suo
.vs/
TestResults/

# Docker
.env.*.local

# Local agent/tool directories
.claude/
.fallow/

# Scratch space for throwaway artifacts (screenshots, render-test dumps, scratch scripts).
# Anything an agent generates that isn't a deliverable goes here and is cleaned up after use.
.scratch/

# Pre-restructure planning notes — kept on disk, out of the repo.
# The current design corpus lives in .claude/docs/ (also gitignored).
server/docs/

# Twitch OpenAPI spec + extraction scripts — a build-time reference for hand-authoring the
# Helix wire DTOs (auto-generated community spec, not a runtime dependency). Kept on disk, out of the repo.
server/.twitch-spec/
server/src/NomNomzBot.Api/wwwroot/*
!server/src/NomNomzBot.Api/wwwroot/.gitignore